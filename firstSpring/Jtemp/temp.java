package Jtemp;

import org.springframework.jdbc.core.JdbcTemplate;

public class temp {

		void fun() {
			JdbcTemplate temp = new JdbcTemplate();
			temp.setDataSource(null);
			
			JdbcTemplate temp1 = new JdbcTemplate();
		}
}
